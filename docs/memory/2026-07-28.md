# Work log — 2026-07-28

## AI Quick Actions Gate G start

- User direction “继续完成” authorizes the final original AI Quick Actions
  gate, Tasks 22–25.
- Created `codex/ai-quick-actions-gate-g` from accepted Gate F.
- Audited the existing Story Kernel generation store/service, ReviewIssue
  persistence, Continuity Engine, snapshots, backup/restore, and Story
  navigation.
- Chose one thin orchestration layer: per-chapter jobs stage into the existing
  Story Kernel conflict batches; AI consistency findings become existing
  ReviewIssue records.
- The extraction ledger will persist operational metadata only and normalize
  interrupted runs to stopped without automatic provider calls after restart.
- Multi-source AI consistency uses exact evidence A/B and optional Story Fact;
  AI severity remains capped at warning and no replacement/edit is returned.
- `ui-ux-pro-max` was read completely. Its referenced search helper is missing,
  so its documented design/accessibility rules remain the direct guide.
- Contract:
  `docs/plans/009-ai-quick-actions-gate-g-extraction-review-hardening.md`.

## Gate G completion

- Implemented an operational-only extraction ledger, sequential runner and
  full-height warm-paper extraction center. Restart converts running work to
  stopped/interrupted and never invokes the provider.
- Reused Story Kernel generation batches for staging, conflicts and selected
  snapshot-backed confirmation.
- Added strict TypeScript/Rust multi-source consistency contracts. Every
  finding needs 2–4 exact selected-source quotes; ReviewIssue now carries
  related evidence and an optional safe Story Fact.
- Safe Story Facts exclude author-secret information, secret relationships,
  character secrets and foreshadowing true meanings.
- Fixed an existing full-suite async race in the Worldbuilding page test by
  awaiting loaded location content.
- `pnpm acceptance`: 75 files / 241 tests. Rust: 44 passed / 2 ignored.
- Visual Gate G: four captures at 1536/1280/1024 with no page overflow,
  unclipped controls and no effective target under 44px.
- Native smoke passed normal close. Portable EXE SHA-256:
  `96E089A88E214BBE739D953E479E18AE9126FD490C3141F794B7BF8FE91EACE2`.
- No release tag: real-key and human-quality gates remain open.

## Project Creation Y0–Y2 completion

- Read the complete
  `Writing_Buddy_Project_Creation_Drag_Highlight_Visual_Handoff_v1.0`
  handoff and enforced its first-run stop after Y2.
- Mapped the existing open path, canonical project ordering, recent-project
  storage, appearance state, and missing creation/drag capabilities.
- Added six typed project templates and a four-step accessible creation
  wizard available before any project is open.
- Added native preflight and verified same-parent staging with atomic rename,
  no-overwrite guarantees, cleanup, and reopen verification.
- Stored theme/accent/writing mode in project-local workspace preferences and
  restored appearance when reopening.
- Added semantic visual state tokens and responsive 1440/1024 layouts.
- Browser acceptance fixed three real polish/accessibility regressions before
  release.
- `pnpm acceptance` passed on Node 24.14.0 / pnpm 10.32.1:
  79 files / 252 tests.
- `cargo test`: 47 passed / 2 ignored; `cargo fmt --check` passed.
- Native isolated startup and normal close passed.
- Portable EXE SHA-256:
  `E366474A76D13DD6A4E3B1354657D54BE17BD8956A77D67BEE0E6D709C5C7C46`.
- Acceptance:
  `docs/acceptance/020-project-creation-y0-y2-2026-07-28.md`.
- Next gate is Y3 chapter/volume drag with keyboard parity, persistence, and
  Undo; Y4 remains blocked behind Y3 acceptance.

## Project structure drag Y3 completion

- Implemented typed drag payloads, targets, move commands, and a central drop
  rule registry for volumes and chapters.
- Added dnd-kit pointer and keyboard sensors, hover/focus handles, semantic
  source/valid/invalid states, insertion markers, constrained overlay,
  live destination feedback, auto-scroll, and Esc cancellation.
- Added native manifest revisions, write-lock enforcement, atomic persistence,
  canonical reopen verification, stale-revision rejection, and rollback on
  failed verification.
- Chapter IDs and Markdown file paths remain stable during same-volume,
  cross-volume, and inverse moves.
- Added a six-second Undo toast, button action, and Ctrl+Z parity outside
  editable surfaces.
- Visual acceptance covered 1440×1000, 1024×768, and 800×720 with zero
  horizontal overflow. It found and fixed the compact sidebar section overlap.
- `pnpm acceptance`: 83 files / 271 tests. Rust: 49 passed / 2 ignored.
- Portable EXE SHA-256:
  `F0A79555B4B83520A4139B92C0DCEB2D2F7D3FE539CD1DA558D8B44458F149C2`.
- Scene cross-chapter movement remains deferred until manuscript-anchor
  migration is designed; Y4 resource association dragging remains the next
  separate gate.

## Project association drag Y4 completion

- Added a dedicated association workbench for characters, items, and
  foreshadowing. Release opens a semantic confirmation surface; it never
  silently writes or invokes AI.
- Kept Story resources on the atomic repository transaction path and item
  transfers on the existing hash-protected item-state store.
- Added revision-safe transaction receipts and inverse Undo for association,
  narrative-order, and plot lifecycle mutations.
- Narrative-order dragging preserves order slots and requires explicit
  confirmation before violating a declared predecessor. Story-time mode stays
  locked with an author-facing reason.
- Plot lifecycle cards now move between columns, reject no-op writes, refresh
  the inspector correctly after reload/Undo, and share the guarded Undo
  interaction.
- `ui-ux-pro-max` rules drove the 44px target, focus, reduced-motion,
  non-color state, and responsive bottom-sheet pass.
- Visual QA covered 1440×1000, 1024×768, and 800×720. It found and fixed the
  narrow Story sidebar overlay; final document/page overflow is zero.
- `pnpm acceptance`: 86 files / 279 tests. Rust: 49 passed / 2 ignored.
- Portable EXE SHA-256:
  `28E534CB52267E469DA6FCE743AB40D979B61573746E3C587A711D018CA79990`.
- Native isolated startup and normal close passed with exit code 0.
- Acceptance:
  `docs/acceptance/022-project-association-drag-y4-2026-07-28.md`.
- Scene cross-chapter movement remains deferred until manuscript-anchor
  migration is designed. The next handoff gate is shared highlight and
  visual-state convergence.

## Project highlight and shared interaction Y5 start

- User explicitly continued after accepted Y4 and supplied a narrow assistant
  screenshot showing a Story Kernel candidate with a generic schema failure.
- The visible raw value `readerVisibility: "hidden"` conflicts with the
  authoritative 0–1 numeric schema; Y5 will expose that exact field/type issue
  without coercing or writing it.
- The original handoff maps the next gate to shared `TreeRow`,
  `DropIndicator`, `DragOverlayCard`, `AssociationMenu`, and `UndoToast`
  primitives plus opt-in entity and AI-state highlights.
- Created `codex/project-highlight-components-y5` from accepted Y4.
- `ui-ux-pro-max` was read completely. Its referenced search helper is still
  absent, so its accessibility, target-size, focus, contrast, motion, and
  responsive rules are applied directly.
- Contract:
  `docs/plans/015-project-highlight-components-y5.md`.

## Project highlight and shared interaction Y5 completion

- Consolidated project-tree and association drag presentation into shared
  TreeRow, DropIndicator, DragOverlayCard, AssociationMenu, UndoToast, and
  AiCandidateFrame primitives without moving domain writes into UI code.
- Added project-scoped, default-off Monaco highlights for character, location,
  item, and foreshadowing MentionLinks. Browser fixtures proved one real
  decoration of each type and four independent controls.
- Reproduced the supplied **信号塔的无声警告** failure with
  `readerVisibility: "hidden"`. It now shows the exact field, expected numeric
  range, safe actual value, localized repair guidance, and blocks acceptance.
- Evidence is labeled and counted; raw provider JSON is diagnostic-only,
  collapsed by default, and internally contained at 1024px.
- Real Settings switches verified Paper, Midnight, Fog, and Focus together
  with Monaco. Document/page overflow was zero and visible candidate actions
  met the 44px rule.
- `pnpm acceptance`: 89 files / 287 tests. Focused Y5 suite: 9 files / 42
  tests. Rust: 49 passed / 2 ignored; format check passed.
- `pnpm tauri:build` produced fresh portable and NSIS outputs. Isolated native
  smoke opened `Writing Buddy`, accepted CloseMainWindow, and exited normally
  with code 0.
- Implementation commit: `830deedd`.
- Portable SHA-256:
  `0B695EC6CAFACB22226A85FE205CEAC1F288814D9D151DE05E8B407D52D4DFE9`.
- Acceptance:
  `docs/acceptance/023-project-highlight-components-y5-2026-07-28.md`.

## AI generation reliability and handoff audit start

- User supplied a native screenshot where Story Kernel generation failed with
  only **AI 生成失败。** and asked whether every design-document feature was
  implemented.
- Root cause: Tauri invoke rejects with a plain structured `PublicAiError`,
  while the Story Kernel runner keeps only `Error` instances and replaces
  other rejection shapes with the generic fallback.
- The same boundary pattern appears in grounded JSON and selection rewrite
  runners; the existing normalizer is coupled to the Zustand AI store.
- Project visual handoff is not complete: Y0–Y5 are accepted but scene drag,
  person/world grouping drag, broad Y6 visual polish, and full Y7 hardening
  remain partial or deferred.
- AI quick-actions functional gates A–G are implemented, but structured error
  recovery, explicit schema-repair retry, and real-key quality/cost acceptance
  remain partial or external.
- Created `codex/ai-generation-reliability-audit`.
- Contract:
  `docs/plans/016-ai-generation-reliability-handoff-audit.md`.

## AI generation reliability and handoff audit completion

- Extracted shared AI error normalization and preserved structured Tauri
  `code`, localized message, retryability, and HTTP status through Story
  Kernel, grounded JSON, and selection-rewrite runners.
- Story Kernel now presents non-color-only failure guidance with either
  **使用相同条件重试** or **检查 AI 设置**. Cancellation remains neutral and
  retry never happens silently.
- Browser fixtures accepted authentication failure, rate-limit then successful
  retry, and unknown rejection in Paper and Midnight at 1440px and 1024px.
  There were no page errors or horizontal overflow, and effective recovery
  controls measured 44px.
- The two design handoffs are not 100% complete. Remaining project work
  includes scene movement, person/world grouping drag, Y6 polish, and Y7
  hardening. Remaining AI work includes the author-visible one-shot schema
  repair request plus real-provider and real-author-project gates.
- `pnpm acceptance` passed 91 files / 295 tests. Rust formatting passed;
  `cargo test` passed 49 tests with two explicit external/release gates ignored.
- The first parallel Tauri package attempt hit the Windows page-file limit.
  Reusing the valid cache with one Cargo job produced fresh portable and NSIS
  artifacts from implementation commit `12a278f7`.
- Isolated native smoke launched PID `35668`, reached input idle, stayed
  responsive with title `Writing Buddy`, accepted `CloseMainWindow`, and
  exited normally with code 0.
- Portable SHA-256:
  `9FB9D5EE103E132B227006F09C6D136468E4E1C3962BA9B4CC0DC3AB554A3DDA`.
- NSIS SHA-256:
  `FA020040D17444519BE3A11328E7808B9B3BC33528329DA2DEB6364CF779DFF3`.
- Acceptance:
  `docs/acceptance/024-ai-generation-reliability-and-handoff-audit-2026-07-28.md`.

## AI prompt contract parity failure

- The user confirmed that the model is usable, but Story Kernel generation
  still returns **AI 配置不完整。** without candidates.
- Connection testing is healthy because it only lists models and checks
  balance. Generation additionally runs the Rust prompt/input security
  contract before network dispatch.
- Commit `830deedd` strengthened the TypeScript Story Kernel prompt for plot
  threads, foreshadowing, and information resources but did not update the
  duplicated Rust prompt. The required byte-for-byte equality therefore fails
  and returns `invalid_configuration` before DeepSeek is called.
- Repair contract:
  `docs/plans/017-ai-prompt-contract-parity.md`.

## AI prompt contract parity completion

- Synchronized the Rust Story Kernel system prompt with the TypeScript prompt
  introduced by `830deedd`; strict pre-dispatch validation remains enabled.
- Added a repository-level parity test for all 14 duplicated TypeScript/Rust
  AI system prompts. The test first reproduced exactly one mismatch, then all
  14 passed after the repair.
- Focused Story Kernel and parity suite passed 23 tests. The full frontend gate
  passed ESLint, TypeScript, 92 files / 309 tests, and the Vite production
  build.
- One unrelated association-page test read an empty fixture during the first
  full run. Its isolated rerun and the complete second run both passed, so no
  unrelated product code was changed.
- Rust formatting passed; 49 tests passed with two explicit external/release
  gates ignored.
- Implementation commit: `9716f473`.
- A fresh one-job Tauri Release build and NSIS package completed. Isolated
  native smoke launched PID `14960`, reached input idle, stayed responsive,
  accepted `CloseMainWindow`, and exited with code 0.
- Portable SHA-256:
  `4FE33EF375DB004864EC1FE34A31937F165DF26B27393DBD6849343EB092B645`.
- NSIS SHA-256:
  `9E9CE1F3F486347CB6422AB14A1B4696FA2D9171A9168B5DFF75B19784EB0E8E`.
- No API key was read and no real paid request was sent.
- Acceptance:
  `docs/acceptance/025-ai-prompt-contract-parity-2026-07-28.md`.

## Project scene movement start

- The user asked to continue after AI prompt parity acceptance, so the next
  documented project-side priority is active on
  `codex/project-scene-movement`.
- The existing volume/chapter command cannot model scenes: the manifest has no
  scene array, while StoryScene and Mention records carry UTF-16 manuscript
  anchors.
- Existing Story resources and Mentions each have multi-record rollback, but
  they are separate from Markdown atomic writes. A scene move therefore needs
  one dedicated native transaction spanning both chapter files and all
  affected JSON resources.
- The contract moves the exact selected manuscript slice, preserves the stable
  scene ID, migrates scene/Mention anchors, updates only Story Positions that
  explicitly reference the moved `sceneId`, and blocks partial Mention
  boundary crossings or stale quotes.
- `ui-ux-pro-max` was read for this increment. Its referenced search helper is
  absent, so keyboard parity, 44px targets, focus, responsive, reduced-motion,
  and non-color-only feedback rules are applied directly.
- Contract: `docs/plans/018-project-scene-movement.md`.

## Project scene movement completion

- Added a deterministic scene-move planner for exact UTF-16 manuscript slices,
  dense scene order, scene/Mention anchor migration, boundary-conflict
  rejection, and explicit scene-specific Story Position migration.
- Added one native `story_commit_scene_move` transaction spanning affected
  Markdown, Story JSON, and Mention JSON with manifest/hash/revision prechecks,
  staging, rollback, verification, and Story index invalidation.
- Undo retains exact pre-move manuscript/resource/Mention state and uses the
  verified post-move hashes/revisions. A reverse drag is insufficient because
  it cannot reconstruct interstitial unlinked manuscript text.
- Added nested scene rows, pointer/keyboard drag, 44px focusable handles,
  search/read-only/dirty disabled explanations, active-editor reload, word
  count refresh, and toast/Ctrl+Z Undo. Dirty manuscript blocks scene movement
  without disabling existing volume/chapter reordering.
- `pnpm acceptance` passed ESLint, TypeScript, 95 files / 321 tests, and the
  Vite production build. Rust formatting passed; `cargo test` passed 51 tests
  with two explicit external gates ignored.
- Browser acceptance passed at 1440x1000, 1024x768, and 800x720 with zero page
  overflow, 44px handles, visible keyboard feedback, cross-chapter move, exact
  Undo, and readable search-disabled feedback.
- The installed browser connector failed to initialize with
  `Cannot redefine property: process`; local Playwright plus installed Edge
  provided the equivalent isolated fixture verification.
- Implementation commit: `405b8742`.
- Final portable SHA-256:
  `E734E721F27EEEFA4725D5C46BFF6A5F99602B9AECC5CC977015472FB67CE860`.
- Final NSIS SHA-256:
  `A4970DF168B0493A4C8FEE58633CC10B22C24A69F06EB47025FA3F09358D73AD`.
- Isolated native smoke launched PID `49044`, reached input-idle, remained
  responsive, accepted `CloseMainWindow`, and exited normally with code 0.
- Acceptance:
  `docs/acceptance/026-project-scene-movement-2026-07-28.md`.

## Project visual polish Y6 start

- Continued from accepted scene movement on
  `codex/project-visual-polish-y6`.
- Read the original project creation/drag/highlight handoff Task 16 directly
  from the read-only source archive. Y6 is a shared visual-system pass, not a
  business-feature rewrite.
- `ui-ux-pro-max` was read completely. Its required `scripts/search.py` is
  absent from the installed package, so its accessibility, interaction,
  responsive, contrast, motion, and icon guidance is applied directly.
- Sanitized browser baseline found 28/34/36-pixel interactive controls, a
  17-pixel status action, two hover translation rules, nine disabled opacity
  overrides, and multiple unrelated empty-state structures.
- The in-app browser connector still fails to initialize in its protected
  runtime. Local Playwright with installed Edge provides equivalent isolated
  browser evidence.
- Contract: `docs/plans/019-project-visual-polish-y6.md`.

## Project visual polish Y6 completion

- Added shared 32/40/44-pixel control tiers, 16/18/20-pixel button icon tiers,
  stable hover feedback, full-opacity disabled treatment, card geometry, and
  reduced-motion rules without changing application behavior.
- Added semantic `AppEmptyState` full/panel/compact variants and migrated the
  major no-resource, search, review, version, reference, continuity, and AI
  candidate/result surfaces.
- Final browser checks passed Paper and Midnight at 1536/1280/1024 with zero
  document overflow, no visible control below the 32-pixel compact floor,
  44-pixel primary actions, visible focus, `5.84:1` Paper disabled contrast,
  and no disabled-opacity regressions.
- Declared the existing SVG application icon as the favicon; a fresh browser
  context then reported zero console errors and zero failed responses.
- Node `24.14.0` with pnpm `10.32.1` passed ESLint, TypeScript, 97 frontend
  files / 327 tests, and the Vite production build. Rust formatting passed;
  51 tests passed with two explicit external gates ignored.
- Implementation commit: `6a00b285`.
- Fresh one-job Tauri Release and NSIS packaging completed.
- Final native smoke launched PID `2984` with an isolated WebView profile,
  reached input idle, remained responsive, accepted `CloseMainWindow`, and
  exited normally with code 0.
- Portable SHA-256:
  `BE5BAB7A930D64DFDFC7D5F7EC724FC1EAA89AE43E444E848C73771C111A7FE0`.
- NSIS SHA-256:
  `640B6A6FEF7E6DD34D92A78498C5C7EDE86E2E72E1A1E208DE1247A1C6139BBC`.
- Acceptance:
  `docs/acceptance/027-project-visual-polish-y6-2026-07-28.md`.
