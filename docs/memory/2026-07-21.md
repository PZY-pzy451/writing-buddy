# 2026-07-21 Engineering Log

## Phase 0.1 — Windows Code-OSS Baseline

- Confirmed repository root: `D:\develop_tool\writing-buddy`.
- Confirmed origin: `https://github.com/microsoft/vscode.git`.
- Confirmed clean working tree before task-created documentation.
- Confirmed branch: `writer/baseline-1.129.1`.
- Confirmed `HEAD` is described by tag `1.129.1` at commit `8a7abeba6e03ea3af87bfbce9a1b7e48fed567b8`.
- Confirmed `.nvmrc` requests Node `24.18.0`.
- Decided to work in place because the user explicitly fixed this project path and the clean target branch already exists.
- The installed `launch` skill's launcher is bash-only and documents macOS/Linux prerequisites. Windows validation will use `scripts\code.bat`, `scripts\code-cli.bat`, isolated runtime directories, and available UI automation.
- The optional PRPM enhancement lookup could not run because `prpm` is not installed; no external skill package was installed.

## Next Step

Run `npm install` with Node 24.18.0 and explicit Python/MSVC settings.

## Environment Findings

- Environment evidence: `D:\develop_tool\build-logs\environment.txt`.
- Windows: Windows 11 Home China, display version 25H2, build 26200, x64.
- Git: 2.55.0.windows.3 at `D:\Tool\Git\cmd\git.exe`.
- Existing system Node/npm: v25.2.1 / 11.6.2; this does not match `.nvmrc`.
- fnm: 1.39.0, with no managed Node version installed yet.
- Python launcher: Python 3.14 x64 at `C:\Users\Admin\AppData\Local\Python\pythoncore-3.14-64\python.exe`.
- Visual Studio Community 2022 17.14.27 is complete and contains `Microsoft.VisualStudio.Component.VC.Tools.x86.x64`.
- Probes found no Windows SDK include root and reported the Windows SDK 26100/22621/19041 and Spectre components as missing.

## Toolchain Preparation

- Installed Node 24.18.0 through fnm; the selected Node version matches `.nvmrc`, with npm 11.16.0.
- First Visual Studio modification attempt exited 87 before installation. Root cause from `vs-installer-attempt-1.log`: Visual Studio Installer 4.3 does not recognize `--wait`.
- Removed only the unsupported argument and reran in quiet mode. The Visual Studio modification completed with exit code 0.
- Verified through `vswhere` that MSVC, Windows 11 SDK 22621/26100, VC Spectre runtimes, ATL Spectre, and ATL/MFC Spectre components are installed.
- Verified SDK include directories `10.0.22621.0` and `10.0.26100.0`, plus MSVC `14.44.35207\lib\spectre`.
- `--includeRecommended` also allowed Visual Studio Installer to fill recommended Native Desktop workload components. No Code-OSS source or product configuration was changed.

## Dependency Installation

- Ran `npm install` with Node 24.18.0, npm 11.16.0, Python 3.14.3, `npm_config_msvs_version=2022`, and explicit Python environment variables.
- The initial install completed with exit code 0 after about seven minutes, adding 1564 root packages and installing 52 build, remote, extension, and test subprojects.
- node-gyp 11.2.0 downloaded and verified Electron 42.6.0 and Node 24.18.0 headers, ending both operations with `gyp info ok`.
- PowerShell 5.1 `Start-Transcript` did not capture native child process output. Re-ran the now-idempotent `npm install` with direct stdout/stderr redirection; it exited 0 and produced `build-logs\npm-install.log` with no blocking failure patterns.
- Added `build-logs/` only to `.git/info/exclude`, keeping generated evidence out of Git without modifying the upstream `.gitignore`.

## Watch Build

- Started `npm run watch` in a durable background process and captured its complete output in `build-logs\watch.log`.
- Confirmed the watch launcher (PID 36760) remained alive after the initial build.
- Main source, Copilot, and built-in extension compiler streams all completed; the log contains 60 explicit `0 errors` markers and no error lines.
- The only build-time warning was Node's `DEP0190` child-process shell deprecation warning; it did not affect compilation.

## Launch Investigation and Resolution

- The first launch attempt stopped before Electron because the temporary fnm multishell `node.exe` produced a Windows SideBySide manifest parse error. The stable fnm installation binary had the same hash and worked, so subsequent launch commands placed `C:\Users\Admin\AppData\Roaming\fnm\node-versions\v24.18.0\installation` first in `PATH`.
- The next `npm run electron` download passed the repository checksum but the extracted `.build\electron` directory lacked the final four ZIP entries (`d3dcompiler_47.dll`, `snapshot_blob.bin`, `electron.exe`, and `ffmpeg.dll`). Defender reported no detection. Because the missing files were exactly the archive tail, this was treated as a transient extraction truncation; rerunning the official Electron task against the verified cached archive produced a complete runtime.
- The successful first launch used `scripts\code.bat` with isolated user-data, extensions, shared-data, workspace, and remote-debugging paths. The workbench, extension host, and agent host stayed alive.
- `scripts\code-cli.bat --version` exited 0 and returned `1.129.1`, `Unknown commit`, and `x64`.

## UI and Runtime Acceptance

- Opened `D:\develop_tool\writing-buddy-test-workspace` in Code-OSS and entered the required Chinese text through the editor. Visual inspection confirmed no missing or duplicated characters, normal line breaks, and normal cursor behavior.
- Code-OSS saved the UTF-8 content successfully. A Save As focus mistake initially derived the filename from the first line; the already-saved file was safely renamed within the same test workspace to `中文输入测试.txt`, without rewriting its contents. A strict ordinal comparison confirmed the required CRLF text and 109-byte file.
- Opened the undocked Developer Tools window and selected Console. The built-in Mermaid extension reported one API-proposal configuration error, and language detection reported `require is not defined` as a warning repeated 12 times. Neither blocked the workbench. After 15 seconds, Console remained at 14 messages, the warning count remained 12, and there were no fatal/crash/unresponsive markers.
- Closed the first application instance through the UI and verified its main process and debug port exited.
- Launched `scripts\code.bat` a second time with the same isolated profile and workspace. The workspace reopened; the explorer listed `中文输入测试.txt`, and opening it displayed all required Chinese content from disk.

## Finalization Decision

- The environment-specific installer, fnm, and Electron extraction failures are recorded in the baseline report and this daily memory. No separate postmortem is needed because no product-code defect or generally reusable repository rule was discovered.
- Full upstream unit/integration suites and packaged/signing flows were not run because Phase 0.1 is specifically the local install, watch-build, source-launch, editor, persistence, DevTools, and CLI baseline.

## Phase 0.2 — Downstream Repository and Minimal Branding

- Preserved Microsoft Code-OSS as `upstream` at `https://github.com/microsoft/vscode.git` and established the validated local bare `origin` at `D:\develop_tool\writing-buddy-downstream.git`.
- Created and worked only on `writer/phase-0.2-minimal-branding`, based on the Phase 0.1 baseline and the committed Phase 0.2 execution contract at `89e244476702f63f9b7283e0f08fbd7e93a799d4`.
- The initial downstream push exposed missing Git LFS objects in the shallow checkout. Fetched all 98 objects reachable from upstream `1.129.1`, then enabled `receive.shallowUpdate=true` only on the local bare repository. The unchanged initial branch push passed; no upstream push occurred.
- Applied the frozen minimal identity only in `product.json`: Writing Buddy names, application/data/shared-data identifiers, Windows mutex/directory/registry/AppUserModel/shell values, URL protocol, and four new Windows AppIds.
- Generated AppIds `94022C55-8340-4119-84FD-40D321AD9EB8`, `4B40CB09-CA9D-4D1E-8D69-99740C497EAD`, `F2B6BC26-EFC4-42EB-85A9-CB6D8122F127`, and `EBAB242E-2896-4B64-B825-C985D9C9761B`. All are valid uppercase, pairwise distinct, different from the Code-OSS originals, and retain the original `{{GUID}` wrapping.
- Ran the official `npm run electron` task. It generated the complete `D:\develop_tool\writing-buddy\.build\electron\Writing Buddy.exe` runtime with Writing Buddy Windows metadata.

## Phase 0.2 Watch Recovery

- Three full Watch attempts failed under Windows commit-memory pressure with `spawn UNKNOWN` or `write ENOMEM`. A resumed attempt still failed after limiting a TypeScript Go subprocess, confirming the machine-wide commit limit rather than source correctness as the root cause.
- Preserved every failure log, did not alter source or reduce the main-source/built-in-extension compilation scope, and paused without a final commit or tag.
- After the pagefile change and reboot, preflight measured a `32.79 GB` commit limit, `16.14 GB` free commit space, and `24.39 GB` free physical memory. Node was reselected to `v24.18.0`, matching `.nvmrc`.
- The recovery preflight script briefly rejected the known ` M product.json` status because its evidence assertion omitted Git's leading status-column space. Direct inspection proved the tree contained only the approved change; the diagnostic was preserved and no repository state was altered.
- The next unmodified full `npm run watch` passed: main/client compilation, no-emit type checking, transpilation, Copilot, and all built-in extensions reported completion with zero blocking errors. Accepted Watch launcher PID `13320` remained alive through both runtime launches.

## Phase 0.2 Runtime Acceptance

- Verified current source support for `--shared-data-dir`, then used the fixed isolated `user-data`, `shared-data`, `extensions`, and `logs` directories under `D:\develop_tool\writing-buddy-runtime`.
- First launch main PID `33152` used debug port `55612`. The UI title contained Writing Buddy, the existing `中文输入测试.txt` opened with all three paragraphs visible, renderer/extension-host processes were present, and the CLI returned exit code 0 with `1.129.1`, `Unknown commit`, and `x64`.
- DevTools opened undocked. The single Mermaid API-proposal error remained at one occurrence over 15 seconds; no increasing fatal/crash/unresponsive loop occurred. Initialization and unauthenticated GitHub messages were informational.
- Closed DevTools and the first workbench normally; the first main PID and debug port exited while Watch stayed alive.
- Second launch main PID `4216` used port `55613` and the same isolated directories. It automatically restored the `writing-buddy-test-workspace`, active Chinese-file tab, and visible content.
- `D:\develop_tool\writing-buddy-test-workspace\中文输入测试.txt` SHA-256 was identical before and after both launches: `8B8ADF29F89F4763B6451F5419C9B4E52273BE0AD2D835CE8136996B20B66AFA`.
- Minor evidence-probe corrections were limited to replacing an unnecessary PowerShell regex escape with literal containment, constraining main-process discovery by debug port and command line, and making CLI validation independent of preceding synchronization/debugger lines. None changed product or test state.

## Phase 0.2 Finalization

- Durable report: `docs/build/windows-writing-buddy-branding-baseline.md`.
- The final commit is restricted to `product.json`, the branding baseline report, `docs/progress.md`, and this daily memory file.
- The final annotated tag is `baseline-writing-buddy-0.2-windows`; branch and tag are pushed only to the local bare origin and verified there.
- Phase 0.3 remained untouched. The only next permitted step is Phase 0.3 — Writer Workbench Layout Prototype from the verified Phase 0.2 tag.

## Phase 0.3 — Writer Workbench Layout Prototype

- Created `writer/phase-0.3-writer-workbench-layout` from the dereferenced Phase 0.2 baseline commit `af858884cd982b88019c30fc76014c3ba0d921a5` and froze the contract before implementation.
- Implemented `extensions/writing-buddy` as a built-in UI extension. Only `build/gulpfile.extensions.ts` changed outside the new extension and durable documentation; no workbench core source was patched.
- Added the static My Novel tree, three Markdown chapters, editor presentation defaults, static scene assistant, live chapter/novel counts, saved state, native focus mode, and restoration behavior.
- Preserved the native TextDocument/Monaco path. The active TextDocument replaces its disk snapshot for counts; TipTap, ProseMirror, AI/model calls, semantic indexing, cloud, accounts, custom formats, and Phase 0.4 work remain excluded.
- Added 43 focused tests covering create-only atomic bootstrap, storage URI conversion, catalog metadata, chapter tracking, assistant escaping/CSP, word-count rules, TextDocument snapshot reading, asynchronous revision safety, workspace identity, ownership disposal, and status presentation.

## Phase 0.3 Runtime Findings

- The first GUI activation failed because desktop extension global storage used the `vscode-userdata:` scheme while create-only sample publication required a local file URI. Core Code-OSS maps the same provider URI to `file:` on desktop. Added the failing regression test first, implemented only that mapping, and reran compile/tests/lint before runtime acceptance.
- The successful run provisioned its sample under `D:\develop_tool\writing-buddy-runtime\user-data\User\globalStorage\writing-buddy.writing-buddy\sample-novel`. The earlier failed activation had already recorded the one-time open state, so the visible Open Sample Novel command was used once; the second launch restored automatically.
- Wide 2560×1392 and narrower 1457×904 checks kept the novel tree, native editor, and assistant usable. Chapter 001/002/003 switching and per-chapter assistant metadata passed.
- Initial counts were 170/532, 171/532, and 191/532. The Chapter 001 sentinel edit changed live counts to 185/547, undo returned 170/532, redo returned 185/547, and save returned `Saved`.
- Entered and exited native Zen Mode; `Ctrl+K`, `Z` restored the complete three-column workbench and status.
- Hot Exit restored an unsaved Chapter 003 Chinese sentence, cursor/view state, workspace, active chapter, assistant, counts 208/564, and `Unsaved`. Removing that sentence and saving returned Chapter 003 to 191 and the final novel total to 547.
- Chapter 001 SHA-256 changed intentionally from `6FFF56968EFE47BB0D6B69644363DCFFB3850D112D5EE5CB0530780274BFF5BD` to `0999A4553924B77BDBABA1776E5B469D8831AE9D7FFB96C022DD654152466D2D`.
- Chapter 002 stayed `158A0A5B6DD66FF9DBE300E94CE81289443FAB007D48ECCF9AE1B9F8738B5228`; Chapter 003 stayed `80AFD388F62F7A312D2120A905B0B6ABA5E190B2FD0D21AF69A05D880E4A2C68`.
- CLI exited 0 with `1.129.1`, `Unknown commit`, and `x64`. The existing one-per-launch Mermaid proposal/tool errors remained non-blocking and were not modified.

## Phase 0.3 Low-Resource Verification

- Ran the final complete, unmodified `npm run watch` alone at `BelowNormal` priority. Main/client no-emit and transpilation, Copilot typecheck/esbuild, extension media, all built-in extensions, and Writing Buddy completed with zero blocking errors. The process stayed alive after the initial build and was then stopped normally.
- Final Watch evidence is `D:\develop_tool\writing-buddy-runtime\logs\phase-0.3-watch-final-low-priority.txt`. Final official Electron evidence is `D:\develop_tool\writing-buddy-runtime\logs\phase-0.3-electron-final-low-priority.txt`; exit code was 0 and `Writing Buddy.exe` is 232,757,248 bytes.
- At Watch peak, free commit space fell to about 9.63 GB. Windows reclaimed it after the task tree exited. Two later samples were stable at 14.74 and 14.79 GB committed of 32.79 GB, about 24.5 GB physical memory available, one Codex tool-server Node process, and roughly 0.04 GB total Node working set.
- A process-tree audit found no Phase 0.3 Watch or Writing Buddy process after shutdown. The symptom was a bounded full-build peak plus delayed commit reclamation, not an unbounded extension leak. No source workaround or reduced compile task graph was introduced.
- Whole-branch review found one Important issue: snapshot reads decoded raw bytes as UTF-8 instead of using Code-OSS TextDocument semantics. A failing adapter test was added first; reads now use `workspace.openTextDocument(...).getText()` while retaining the active override and revision guard.
- Focused checks after the review fix: extension compile 0 errors, 43 tests passing, ESLint exit 0.
- Durable acceptance report: `docs/build/windows-writing-buddy-workbench-layout.md`. Remaining steps are whole-branch review, fresh final verification, documentation commit, clean tree, local bare-origin push, annotated `baseline-writing-buddy-0.3-windows` tag, and exact ref comparison.
