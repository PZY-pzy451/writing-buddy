# 2026-07-27

## Decisions

- Keep the Phase 1.0A provider surface deliberately narrow: one DeepSeek BYOK
  provider and one isolated StoryForge test flow.
- Keep provider HTTP, credential reads, streaming, retries, timeouts, and
  cancellation in Rust. React receives only scoped commands and public events.
- Accept only current runtime model identifiers returned by `/models`;
  `deepseek-v4-flash` and `deepseek-v4-pro` are the local preferences, while
  retired `deepseek-chat` and `deepseek-reasoner` identifiers are rejected.
- Persist only non-sensitive preferences, a short-lived model cache, and
  anonymous token usage. Never persist prompts, outputs, private reasoning, or
  key material.
- Hide the task dock and assistant column on system pages so AI Settings and AI
  Test keep a usable desktop canvas.
- Keep real-key validation and M9 human cutover as explicit manual gates.

## Verification

- Node `24.14.0`, pnpm `10.32.1`
- ESLint, TypeScript, Rust formatting, and diff hygiene passed
- Vitest: 8 files / 21 tests passed
- Rust: 14 tests passed
- Vite and production Tauri/NSIS builds passed
- Final packaged EXE remained alive during a 5-second smoke launch
- Secret scan found no key or bearer-token pattern

## Artifacts

- Frozen design:
  `docs/superpowers/specs/2026-07-26-phase-1.0a-deepseek-ai-foundation-design.md`
- Implementation plan:
  `docs/superpowers/plans/2026-07-26-phase-1.0a-deepseek-ai-foundation.md`
- Acceptance:
  `docs/acceptance/003-phase-1.0a-ai-foundation-2026-07-27.md`
- EXE:
  `apps/desktop/src-tauri/target/release/writing-buddy-next.exe`
- Installer:
  `apps/desktop/src-tauri/target/release/bundle/nsis/Writing Buddy_0.1.0_x64-setup.exe`

## Open work

- Run and record the real DeepSeek key workflow.
- Continue the existing M9 human cutover sessions.

## StoryForge Gate A

- Imported the complete `storyforge_handoff` package and froze the source map
  against baseline `7e658a284920b0e61d42da39a0addaa39bd6b11d`.
- Kept project opening in the existing Zustand store, DesktopBridge, Rust
  command/migration modules and process-lock service. No second router or
  parallel project architecture was created.
- Replaced generic project-open failure handling with structured public
  code/stage/capability/diagnostic data and conditional recovery actions.
- First launch without a recent project now remains on a stable landing page
  instead of immediately opening a native folder picker.
- The canonical release opened the sanitized 6-chapter project, recovered the
  same chapter after a controlled abnormal stop and stale lock, and left all
  17 managed files byte-identical.
- Measured process start to write-lock completion at 1,104 ms initially and
  1,452 ms during second-launch stale-lock recovery.
- Gate A evidence:
  `docs/acceptance/005-storyforge-gate-a-project-open-2026-07-27.md`.
- Stop here for human Gate A approval. Story Kernel remains forbidden until
  that approval.

## Window close follow-up

- Reproduced the canonical release ignoring the native title-bar Close button.
- Tauri 2.11.1 `onCloseRequested` calls `window.destroy()` after the handler
  allows exit, but `core:window:default` does not grant the destroy command.
- Added only `core:window:allow-destroy` to the main-window capability and a
  Rust regression test that fails if this permission is removed.
- Re-ran Node 24 acceptance (12 files / 37 tests), Rust (20 tests), and the
  complete Tauri/NSIS release build.
- An isolated rebuilt process exited after a normal non-force termination
  signal; the old release remained running under the same check.
- Evidence:
  `docs/acceptance/006-window-close-regression-2026-07-27.md`.
- Durable lesson:
  `docs/postmortem/003-tauri-close-capability.md`.

## Phase 1.0B follow-up

- Root cause of the short system pages: when WriterHeader returned `null`, CSS
  Grid auto-placed the system canvas into the fixed editor-header row.
- System routes now render only a single full-height canvas row. Their scroll
  surface fills the available center width, while direct children share a
  responsive 1440–1480px content boundary.
- Review now supports Local manual and AI automatic modes. AI receives only the
  current chapter after a direct click; suggestions remain author-confirmed.
- Rust distinguishes `storyforge-test` from `chapter-review`, validates a fixed
  contract for each, and records only the anonymous job type.
- Final verification: 9 Vitest files / 27 tests, 15 Rust tests, Vite, isolated
  Tauri/NSIS release build, three responsive viewport checks, no console
  warnings/errors, and no horizontal overflow.
- The canonical release EXE was open in a user-owned window. It was not
  terminated; an isolated Cargo target completed the release build instead.
- Acceptance:
  `docs/acceptance/004-system-layout-and-ai-review-2026-07-27.md`
- Postmortem:
  `docs/postmortem/002-running-release-artifact-lock.md`

## StoryForge Gate B

- Human approval to continue arrived as “Story Kernel 还未完成”; Tasks 2–8 were
  implemented as independent commits and Gate C was not started.
- Added `packages/story-kernel` with branded IDs, narrative positions,
  evidence, schemaVersion 1 Zod schemas, safe path mapping, repository
  contracts and transactions.
- Added narrow Rust resource/Mention commands with batch validation, atomic
  replace, revision conflicts, Trash restore and existing snapshot inclusion.
- Integrated all 12 Story resource types into the existing Zustand
  route/tab architecture; missing resources use an explicit recovery view.
- Added the professional project dashboard, Scene navigator/gutter markers,
  external Mention persistence, six selection actions, Monaco decorations and
  resource backlinks.
- Full-suite review caught old tests that still expected direct chapter open;
  they now verify Dashboard first and then continue to the chapter.
- Real 1536×992-equivalent Tauri capture caught a CSS cascade regression:
  `.is-dock-closed` overrode the Dashboard one-row layout and reduced it to
  90px. Moving the Dashboard override after the editor rows restored the full
  852px workspace; a CSS contract test now protects ordering.
- The same real project showed 3 Mention files were loaded by Rust but not
  painted when they resolved before Monaco mounted. An `editorReady` replay
  dependency fixed the race; final evidence has 3 Mention decorations and
  2 Scene glyphs.
- Final verification: Node 24 / pnpm 10, ESLint, TypeScript, 21 Vitest files /
  80 tests, 27 Rust tests, Vite, production Tauri and NSIS all passed.
- The sanitized 6-chapter acceptance copy retained all 17 original files
  byte-identically while adding 5 resources, 2 scenes and 3 mentions under
  `story/`. Compatibility remained 10/10 with 0 blocking fixtures.
- Real local interaction measured 66ms to open Dashboard and 114ms to open the
  chapter with all Story annotations. All three target viewports had zero
  horizontal page overflow.
- Final evidence:
  `docs/acceptance/007-storyforge-gate-b-story-kernel-2026-07-27.md`.
- Stop here for explicit Gate B approval. Task 9 and Gate C remain forbidden.

## StoryForge Gate C

- Human approval arrived as “开始下一阶段开发”; Tasks 9–12 were implemented
  and Task 13 was not started.
- Character resources now carry the full static profile, while StateRecord
  provides position-aware location, survival, injury, emotion, goal, inventory,
  knowledge, misconception and ability history with confirmation/conflicts.
- Relationship resources are directed and time-bounded. The UI shares one
  time-slice model across an accessible matrix and a Worker-laid-out graph,
  capped at 150 nodes and 500 edges.
- Timeline events separate story time from narrative order, support five time
  certainty modes, virtual tracks, zoom, keyboard navigation, a list fallback
  and an edit Inspector.
- Pure deterministic rules detect impossible overlap, predecessor inversion and
  insufficient travel time, and map both evidence positions into ReviewIssue.
- Visual acceptance found the same short-scroll-area pattern reported by the
  user: an absent error banner still shifted the workspace into an auto-sized
  CSS Grid row. Always-present zero-height message slots restored the timeline
  and relationship workspaces to the full remaining height.
- A higher-specificity Story Studio grid also defeated the compact breakpoint.
  Explicit Story Studio media rules now produce 304px, 280px and 240px project
  sidebars at the three target sizes.
- Final Node 24 acceptance: 29 Vitest files / 98 tests, ESLint, TypeScript and
  Vite passed. Rust formatting and 27 Rust tests passed. Compatibility remained
  10/10 with zero blocking fixtures.
- The sanitized 6-chapter Gate C copy contains 3 characters, 4 states,
  3 relationships and 3 events; all six Markdown hashes are unchanged.
- An isolated release and NSIS installer were built because the canonical EXE
  remains open in a user-owned process. The prior Codex-owned isolated instance
  closed normally, and the final build is running as PID 43128.
- Final evidence:
  `docs/acceptance/008-storyforge-gate-c-character-relationships-timeline-2026-07-27.md`.
- Stop here for explicit Gate C approval. Task 13 and Gate D remain forbidden.

## StoryForge Gate D

- Human approval arrived as “继续完成”; Tasks 13–16 were implemented and Task
  17 was not started.
- Locations now form validated hierarchies with map points, travel links,
  faction membership, rules and evidence. Factions and world rules have
  structured first-class schemas and professional editors.
- Items are narrative assets with position-aware holder, location, quantity,
  condition and transfer history. Pure rules detect negative quantity,
  overlapping transfer intervals and unique items with multiple holders.
- Plot threads use five lifecycle states. Foreshadowing records planting,
  reminders, planned/actual payoff, reader visibility and evidence, with
  overdue-risk output.
- StoryInformation separates objective truth from reader reveal. KnowledgeState
  resolves reader and character knowledge at a narrative position, including
  incorrect beliefs. Author secrets default to AI exclusion.
- Visual review at all three responsive tiers found the 1024px knowledge matrix
  evidence column was clipped. The compact layout now keeps the three critical
  columns without page-level overflow.
- Final Node 24 acceptance: 38 Vitest files / 110 tests, ESLint, TypeScript and
  Vite passed. Rust formatting and 27 Rust tests passed. Compatibility remained
  10/10 with zero blocking fixtures.
- The copied six-chapter Gate D project kept all Markdown hashes unchanged.
  Six responsive screenshots and machine-readable metrics were recorded.
- An isolated production Tauri release was built and launched successfully;
  the final `Writing Buddy` window is responsive.
- Final evidence:
  `docs/acceptance/009-storyforge-gate-d-world-assets-plots-information-2026-07-27.md`.
- Stop here for explicit Gate D approval. Task 17 and Gate E remain forbidden.

## StoryForge Gate E

- Human approval arrived as “继续完成”; Tasks 17–20 were implemented and Task
  21 was not started.
- Context Pack construction is deterministic, priority/token-budget aware,
  deduplicated and author-visible. Author secrets are excluded by default.
- Grounded selection rewrite binds candidates to source range and revision,
  streams DeepSeek output, exposes Diff/rationale/impact, supports partial or
  full author accept and reuses the existing edit transaction for Undo.
- P3 character context now resolves StateRecord values at the selected scene's
  narrative order and distinguishes confirmed, pending and conflicting state.
- AI story extraction is JSON-only and writes pending facts atomically under
  `.writing-buddy/ai/`; no fact becomes confirmed without per-item author
  acceptance, Evidence and StoryPosition.
- Long-form continuity review aggregates text rules, deterministic Story Kernel
  checks and AI suggestions with stable deduplication, stale evidence,
  two-source navigation and persisted resolution. AI-only issues are capped at
  warning.
- Visual acceptance found no page-level overflow at 1536×992, 1280×800 or
  1024×720. The compact view uses an internal full-height scroll surface and
  avoids the previously reported short scroll strip/large blank region.
- Final Node 24.14.0 acceptance: 45 Vitest files / 131 tests, ESLint,
  TypeScript and Vite passed. Rust formatting and 29 tests passed.
  Compatibility remained 10/10 and copied-project Markdown hashes remained
  6/6 unchanged.
- The first Gate E process closed normally. A fresh Node 24 Tauri/NSIS build
  launched and restored `脱敏测试作品 10` plus the continuity page on second
  start; the final instance remains running.
- Final evidence:
  `docs/acceptance/010-storyforge-gate-e-grounded-ai-continuity-2026-07-27.md`.
- Stop here for explicit Gate E approval. Task 21 and Gate F remain forbidden.

## StoryForge Gate F / Task 21

- Human approval arrived as “继续完成”; Gate F is now active.
- Added a deterministic derived Story Index with bounded queries, incremental
  upsert/remove, on-disk cache rebuild/load/invalidation and no manuscript
  content in the persisted snapshot.
- Character and timeline lists now render bounded windows; the timeline canvas
  virtualizes both event columns and track rows.
- The deterministic scale gate covers 1,000 chapters, 10,000 scenes, 5,000
  resources, 50,000 events/states and 200,000 mentions.
- Release metrics: project interactive 420.4278 ms, resource switch 0.5909 ms,
  single-resource save 0.0046 ms, timeline feedback 4.2342 ms and filtered
  graph 0.192 ms. Every metric is below its frozen threshold.
- Node 24.14.0 acceptance passed: 47 Vitest files / 136 tests, ESLint,
  TypeScript and Vite. Rust formatting and 33 tests passed; the release-only
  scale test is intentionally ignored during the normal Rust suite.
- Continue with Task 22 responsive, accessibility and visual acceptance.

## StoryForge Gate F / Task 22

- Reproduced the user's large-blank-region issue at exactly 1280px: the hidden
  assistant still reserved a 320px grid column and reduced the dashboard from
  922px to 602px. The assistant-closed breakpoint now begins at 1280px.
- The 1024px Story Kernel sidebar now owns a full-height scroll region; the
  Continuity Review action and footer remain reachable instead of being
  clipped below the viewport.
- StoryForge controls now use 44px targets. Modal focus is trapped and restored,
  Story navigation and relationship matrices support arrow keys, reduced motion
  is enforced and muted theme text meets WCAG AA contrast.
- Eight browser captures cover the six approved prototypes plus 1280 and 1024
  responsive evidence. Every capture has zero page overflow, inaccessible
  clipped controls and visible StoryForge controls below 44px.
- Node 24.14.0 acceptance passed: 48 Vitest files / 141 tests, ESLint,
  TypeScript and Vite.
- Continue with Task 23 migration, backup and recovery acceptance.

## StoryForge Gate F / Task 23

- Added a pure staged v0-to-v1 Story Resource migration with validated commit,
  exact rollback and no implicit filesystem writes or input mutation.
- Version snapshots and `.wbbackup` archives include canonical Story Kernel
  resources and pending author-reviewed facts while excluding derived indexes,
  runtime state and AI temporary/cache data.
- Restore clears rebuildable caches and invalidates the in-memory/disk Story
  Index before reopening canonical project data.
- The sanitized six-chapter recovery drill restored 47/47 included entries,
  preserved 6/6 manuscript hashes, resolved 89/89 Story backlinks and reopened
  the writable project twice with the same project ID.
- Node 24.14.0 acceptance passed: 49 Vitest files / 145 tests, ESLint,
  TypeScript and Vite. Rust formatting and 33 normal tests passed; both ignored
  release gates passed separately.
- Vitest worker concurrency is capped at four to prevent Windows process-fork
  resource exhaustion during the combined acceptance command.
- Continue with Task 24 final vertical-slice and production acceptance.

## StoryForge Gate F / Task 24

- Added an end-to-end professional slice covering resource creation, directed
  relationships, item transfer, deterministic review, grounded AI rewrite,
  author accept/Undo, snapshot, backup, restart and restore.
- Added user operation/build guidance and the durable Story Kernel architecture
  document.
- Final Node 24.14.0 acceptance passed: ESLint, TypeScript, Vite, 50 Vitest
  files and 146 tests.
- Rust formatting passed. The normal suite passed 33 tests with two explicit
  release gates ignored; both performance and sanitized recovery gates passed
  separately.
- The 266,001-record scale gate remained below all five thresholds. Recovery
  restored 47/47 entries, 6/6 chapter hashes and 89/89 backlinks.
- A fresh isolated Tauri release and NSIS installer built successfully. The
  first process accepted normal close and exited; the second launch restored
  `project-0000000a`, acquired a read-write lock and rebuilt/loaded the Story
  Index.
- Windows screenshot capture was occluded by an unrelated exclusive full-screen
  process. Further UI input was stopped to avoid affecting it; the eight Gate F
  real-project browser captures remain the visual evidence.
- Gate F is complete. Create the professional baseline tag, push branch/tag and
  stop for explicit product direction.

## AI Story Kernel direct generation

- New product scope was explicitly provided after Gate F: allow AI to generate
  complete Story Kernel resources directly.
- The interpretation is structured direct generation with mandatory author
  confirmation, not silent AI writes. This preserves the accepted trust
  boundary and atomic repository guarantees.
- Created branch `codex/ai-story-kernel-generation` and froze request,
  response, staging, conflict, confirmation, UI and acceptance contracts in
  `docs/plans/001-ai-story-kernel-generation.md`.
- Next: implement the isolated AI job purpose and strict generation parser
  before candidate storage or UI.

## AI Story Kernel direct generation completion

- Added the isolated `story-kernel-generation` job contract in TypeScript and
  Rust. Requests are bounded to explicit instructions, chapter/selection
  source, selected types and non-secret resource identities.
- Added strict parsing for complete resources across character, scene,
  location, faction, item, world rule, timeline event, relationship, plot
  thread, foreshadowing and information types.
- Added `.writing-buddy/ai/story-kernel-generation/index.json` pending batches
  with application-owned system fields/evidence, strict schema hydration,
  duplicate/collision/missing-reference/dependency blocking and persisted
  accept/reject decisions.
- Added `expectedAbsent` from StoryTransaction through browser/Tauri storage so
  AI create operations can never overwrite a resource created after staging.
- Added the writing assistant **Kernel** tab with 44px controls, source/privacy
  disclosure, type selection, streaming/cancel state, evidence/conflict/JSON
  review, rejection and snapshot-backed atomic batch confirmation.
- Pending generation batches now participate in snapshots and `.wbbackup`
  recovery while AI temp/cache paths remain excluded.
- `pnpm acceptance` passed: 52 files / 153 tests. Rust formatting and 35 normal
  tests passed; two explicit release-only tests were ignored by the normal
  suite.
- Release performance remained below every threshold: project interactive
  441.7083 ms, resource switch 0.7005 ms, single save 0.0056 ms, timeline
  feedback 4.2447 ms and filtered graph 0.1968 ms.
- Sanitized recovery restored 47/47 entries, preserved all six manuscript
  hashes, resolved 89/89 backlinks and passed second launch.
- Fresh production artifacts were built under
  `tmp/ai-story-kernel-target-final`. Normal close succeeded and the second
  launch (PID 13496 at verification time) was responsive.

## AI Quick Actions Gate A/B start

- The user supplied
  `Writing_Buddy_StoryForge_AI_Quick_Actions_Handoff_v1.0` and two warm-paper
  professional editor references.
- The handoff explicitly limits the first execution to Gate A and Gate B.
  Concrete editor, character, relationship, world, timeline, item, plot and
  foreshadowing actions must not be implemented in this round.
- Created `codex/ai-quick-actions-gate-ab` from the completed Story Kernel AI
  generation commit.
- Froze the real runtime/source map and capability matrix in plans 002 and 003.
  Existing DeepSeek transport, selection context, Story generation, snapshots
  and repositories will be adapted rather than duplicated.
- Baseline passed: 52 Vitest files / 153 tests, TypeScript, ESLint and 35 normal
  Rust tests. Two explicit release gates remained ignored. The current shell
  uses Node 25.2.1 while the repository requests Node 24, so pnpm emitted an
  engine warning without affecting the baseline result.

## AI Quick Actions Gate A/B completion

- Gate A commit `3305500` adds the real Source Map, capability matrix, Action
  Registry, versioned Prompt Registry, strict structured-output validator, and
  the framework-independent `@writing-buddy/ai-actions` package.
- Gate B commit `962b713` adds privacy-aware Context Packs, visible consent,
  a global generation state machine, the responsive unified Drawer, and
  snapshot/revision/rollback Preview Transactions.
- The real integration action is only `review.consistency`; all concrete
  writing and Story resource actions remain outside the approved scope.
- Final review corrected the Context Preview so it lists exactly the current
  chapter accepted by the fixed Rust `chapter-review` request contract rather
  than claiming project or scene metadata is sent.
- Visual acceptance at 1536, 1280, and 1024 recorded zero page overflow,
  clipped interaction controls, or Drawer controls below 44px.
- Final `pnpm acceptance` passed 60 files / 176 tests. Rust formatting passed;
  35 normal tests passed with two explicit release gates ignored.
- A fresh isolated Tauri EXE and NSIS installer were built. PID `50596`
  remained responsive during smoke and exited after a normal close request.
- Evidence:
  `docs/acceptance/013-ai-quick-actions-gate-ab-2026-07-27.md`.
- Stop here for explicit human approval before Gate C.

## AI Quick Actions Gate C start

- The user explicitly approved continuation and supplied a close reference of
  the selected-text toolbar, highlighting **AI 润色** and **创建人物** as missing
  real workflows.
- Created `codex/ai-quick-actions-gate-c` from the accepted Gate A/B branch.
- Gate C will adapt the existing grounded Selection Rewrite and Story Kernel
  generation services rather than create new providers or write paths.
- Clicking an action will open the correct assistant workflow but will not
  automatically start a billed DeepSeek request.
- The active contract is
  `docs/plans/004-ai-quick-actions-gate-c-editor-actions.md`.

## AI Quick Actions Gate C completion

- Added transient assistant intents so the selected-text toolbar opens a
  concrete rewrite action or one-type Story Kernel preset.
- **AI 润色** now opens the grounded selection-rewrite workflow. The assistant
  also exposes polish, concise, expand, grammar, dialogue, and pacing actions.
- Character, location, item, information, and foreshadowing buttons now open
  candidate generation with the current selection and an author-visible
  instruction. Preset application does not make a paid request.
- Added `expand` to the TypeScript Context Pack and fixed Rust selection
  request contract.
- Preserved Diff, edited/full acceptance, rejection, stale protection, Undo,
  snapshot-backed Story Kernel confirmation, atomic writes, and author-secret
  exclusion. Added rewrite cancellation.
- Applied the UI/UX skill's 44px, visible-state, contrast, focus, and responsive
  guidance to the warm-paper selection toolbar and assistant surfaces.
- Visual acceptance at 1536, 1280, and 1024 recorded zero page overflow,
  clipped controls, or effective interaction targets below 44px.
- `pnpm acceptance` passed 62 files / 181 tests. Rust formatting passed; 35
  normal tests passed with two explicit release gates ignored.
- A fresh isolated Tauri EXE and NSIS installer were built under
  `tmp/ai-quick-actions-gate-c-target-final`. PID `44584` remained responsive
  during smoke and exited after a normal close request.
- Evidence:
  `docs/acceptance/014-ai-quick-actions-gate-c-2026-07-27.md`.

## AI Quick Actions Gate C2 start

- The user asked to continue after the selected-text Gate C acceptance.
- The original handoff still has two incomplete Gate C tasks: AI continuation
  and scene goal/outline/emotion-beat candidates.
- Created `codex/ai-quick-actions-gate-c2` from the accepted Gate C branch.
- The required `superpowers:subagent-driven-development` and
  `superpowers:executing-plans` skills referenced by the handoff are not
  installed. Work continues locally with the same test-first, per-gate,
  durable-evidence workflow.
- Scene planning will update current StoryScene fields rather than rewrite
  `project.yaml`; the detailed contract is
  `docs/plans/005-ai-quick-actions-gate-c2-continuation-outline.md`.

## AI Quick Actions Gate C2 completion

- Added fixed `manuscript-continuation` and `scene-plan-generation` job
  purposes to the TypeScript and Rust boundary. Both require bounded grounded
  context and reject path/key/credential fields.
- Added header continuation modes for continuing a paragraph, completing an
  existing scene, and generating exactly three directions.
- Continuation candidates capture cursor offset, manuscript revision, and
  local anchors. Only the selected candidate is inserted; stale/rejected
  candidates are blocked and accepted insertion supports Undo.
- Added the assistant **细纲** tab for scene goal, outline, reverse extraction,
  and emotion beats. Every returned StoryScene field is independently
  selectable.
- Scene-plan apply rechecks the current repository resource before snapshot,
  then uses the expected StoryScene revision and saves only selected fields.
- Added optional backward-compatible `emotionBeats` to StoryScene.
- Applied warm-paper UI hierarchy, visible states, Lucide icons, 44px targets,
  and assistant scrolling at 1536/1280/1024. Visual metrics recorded zero page
  overflow, clipped controls, or undersized targets.
- `pnpm acceptance` passed 64 files / 188 tests. Rust formatting passed; 37
  normal tests passed with two explicit release gates ignored.
- A production EXE and NSIS installer were rebuilt under the accepted
  `tmp/ai-quick-actions-gate-c-target-final` dependency cache with
  `CARGO_BUILD_JOBS=1`. PID `48200` remained responsive and exited on normal
  close.
- A cold parallel release build exhausted memory and corrupted dependency
  metadata. Durable prevention is recorded in
  `docs/postmortem/004-cargo-release-memory-pressure.md`.
- Acceptance:
  `docs/acceptance/015-ai-quick-actions-gate-c2-2026-07-27.md`.

## Gate D resumed

- User direction “继续完成” authorizes continuing the original handoff into
  Gate D.
- Created `codex/ai-quick-actions-gate-d` from accepted Gate C2.
- Froze dedicated `character-analysis` and `relationship-analysis` contracts,
  exact evidence, normalized deduplication, explicit same-name merge,
  selected-only confirmation, directed relationship semantics and dashed
  pending edges in plan 006.
- The old Code-OSS repository was briefly opened because the desktop workspace
  pointed there. No files were changed; its original branch and dirty worktree
  were preserved before continuing in `writing-buddy-next`.

## AI Quick Actions Gate D completion

- Added strict TypeScript/Rust character and directed-relationship analysis
  contracts plus seven dedicated Action Registry actions.
- Character Center now generates exactly three profile candidates, enriches
  background/arc/voice, and extracts evidence-backed facts and dynamic state
  from one explicitly selected chapter.
- Same-name matches and conflicting fields require explicit author choices;
  apply is selected-only, source/revision checked, and snapshot backed.
- Relationship Graph now generates independent directional candidates and
  extracts relationship evolution. Pending edges are dashed, offset by
  direction, and accepted one edge at a time.
- The UI/UX skill influenced the warm-paper hierarchy, Lucide icon usage,
  44px targets, visible states/focus, reduced-motion handling, and responsive
  drawer behavior.
- Visual checks at 1536/1280/1024 found zero page overflow, clipped controls,
  or undersized Story controls.
- `pnpm acceptance` passed 66 files / 199 tests. Rust formatting passed; 39
  tests passed with two explicit release gates ignored.
- A fresh production EXE and NSIS installer were built with one Cargo job. PID
  `8276` remained responsive and exited on a normal close request.
- Acceptance:
  `docs/acceptance/016-ai-quick-actions-gate-d-2026-07-27.md`.

## AI Quick Actions Gate E start

- User direction “jixu” continues the original handoff into Tasks 15–17.
- Created `codex/ai-quick-actions-gate-e` from accepted Gate D.
- Froze dedicated `world-analysis` and `item-analysis` contracts, typed world
  candidate schemas, mandatory generated-rule scope/exception review,
  same-name explicit merge, selected-only item states, deterministic
  unique-item conflict checks, snapshot/revision checks, and exact extraction
  evidence in plan 007.
- The `ui-ux-pro-max` rules file was read completely. Its referenced search
  script is absent from the installed skill package; the documented design and
  accessibility rules remain the direct implementation guide.

## AI Quick Actions Gate E completion

- Added strict TypeScript/Rust `world-analysis` and `item-analysis` contracts,
  eight dedicated Action Registry actions, and sanitized browser fixtures.
- Worldbuilding now generates typed entries or extracts grounded candidates
  from one selected chapter. Same-name merges, per-field conflicts, exact
  evidence, stale resources, and scoped rule conflicts are author-visible.
- Items now generates cards and history or extracts cards and state events.
  Card fields and events are accepted separately; deterministic rules block
  overlapping holders for unique items and close a valid prior interval on
  transfer.
- Both guarded apply paths recheck source and resource revisions, snapshot
  before writing, and persist selected values only.
- The UI/UX skill influenced the warm-paper hierarchy, Lucide icon usage,
  44px targets, visible focus/states, and independently scrolling responsive
  drawers.
- Visual checks at 1536/1280/1024 found zero page overflow, clipped controls,
  or undersized Story controls.
- `pnpm acceptance` passed 69 files / 210 tests. Rust formatting passed; 41
  tests passed with two explicit release gates ignored.
- A fresh production EXE and NSIS installer were built with one Cargo job. PID
  `9036` remained responsive and exited on a normal close request.
- Acceptance:
  `docs/acceptance/017-ai-quick-actions-gate-e-2026-07-27.md`.
