# 2026-07-26

## Decisions

- Keep Legacy read-only and perform every write test on explicit copies.
- Keep strict CSP and local Tauri content. Disable Tauri prototype freezing
  because Monaco assigns `KeyCodeUtils.toString` during module initialization.
- Preserve session immutability at the Zustand boundary using
  `DocumentSession.copy()` so dirty state and word counts rerender together.
- Keep WebView remote-debugging configuration under `scripts/diagnostics`;
  production `tauri.conf.json` contains no remote-debugging argument.
- Do not mark human M9 gates complete and do not retire Legacy.

## Verification

- Node `24.14.0`, pnpm `10.32.1`
- ESLint and TypeScript passed
- Vitest: 6 files / 13 tests passed
- Compatibility: 10/10, blocking 0
- Cargo: 3/3 passed
- Vite and production Tauri/NSIS builds passed
- Real desktop workflow passed save, conflicts, resources, review,
  Credential Manager, versions, backup, restart, themes, CRLF and BOM
- Corrupted projects and invalid backups were rejected without source writes

## Artifacts

- Acceptance:
  `docs/acceptance/002-desktop-acceptance-2026-07-26.md`
- EXE:
  `apps/desktop/src-tauri/target/release/writing-buddy-next.exe`
- Installer:
  `apps/desktop/src-tauri/target/release/bundle/nsis/Writing Buddy_0.1.0_x64-setup.exe`

## Open work

- Complete and record the human M9 cutover sessions.
